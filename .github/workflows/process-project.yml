name: project-automation

on:
  project_card:
    types: [ created, moved, converted, edited, deleted ]

# Backlog = 8849132
# Scheduled = 8849133

jobs:
  process-project-update:
    runs-on: ubuntu-latest
    steps:
      - name: Process card movement to Backlog
        if: github.event.action == 'moved'
        run: |
          echo "Actor: ${{ github.actor }}"
          echo "Event: ${{ github.event.action }}"
          echo "From Column: ${{ github.event.changes.column_id.from }}"
          echo "To Column: ${{ github.project_card.column_id }}"
          echo "Issue URL: ${{ github.projct_card.content_url }}"
              
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump job context
        env:
          JOB_CONTEXT: ${{ toJson(job) }}
        run: echo "$JOB_CONTEXT"
